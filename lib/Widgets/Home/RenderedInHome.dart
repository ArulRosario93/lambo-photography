import 'package:flutter/material.dart';

class RenderedInHome extends StatelessWidget {
  const RenderedInHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}